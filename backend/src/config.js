const config = {
    development: {
        database: {
            url_connection: 'URL_CONNECTION_HERE',
        },
        server: {
            port: 'PORT_NUMBER_HERE',
        }
    }
};

module.exports = config;